#
# LDAP Defaults
#

# See ldap.conf(5) for details
# This file should be world readable but not world writable.

TLS_CACERTDIR   /etc/openldap/certs
BASE            dc=sudo,dc=role
BINDDN          cn=sudo,ou=Administrators,dc=sudo,dc=role
BINDPW          sudo
URI		{{ sudo_server }}
TIMELIMIT       15
TIMEOUT         20
SUDOERS_BASE    ou=sudoers,dc=sudo,dc=role
SUDOERS_DEBUG   0

# Turning this off breaks GSSAPI used with krb5 when rdns = false
SASL_NOCANON 
